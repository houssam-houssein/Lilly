rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Public menu: anyone can read. Only signed-in users can write (use a single admin account).
    match /lilySettings/{docId} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
